# @managrsphere/capacitor-apple-maps

Apple Maps Plugin similar to the official Google Maps Capacitor Plugin

## Install

To use npm

```bash
npm install @managrsphere/capacitor-apple-maps
````

To use yarn

```bash
yarn add @managrsphere/capacitor-apple-maps
```

Sync native files

```bash
npx cap sync
```

## API

<docgen-index>

* [`echo(...)`](#echo)

</docgen-index>

<docgen-api>
<!--Update the source file JSDoc comments and rerun docgen to update the docs below-->

### echo(...)

```typescript
echo(options: { value: string; }) => Promise<{ value: string; }>
```

| Param         | Type                            |
| ------------- | ------------------------------- |
| **`options`** | <code>{ value: string; }</code> |

**Returns:** <code>Promise&lt;{ value: string; }&gt;</code>

--------------------

</docgen-api>
